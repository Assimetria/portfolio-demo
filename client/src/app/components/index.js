// @system/@custom — Components barrel (auto-generated)
// Re-exports all components from @system and allows @custom overrides.
// @system components are available by default. @custom components can override.

// All system components
export * from './@system'

// Custom overrides (if any)
// Note: import order matters — @custom should be imported last to override @system
export * from './@custom'
